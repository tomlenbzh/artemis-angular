import { EthicsDirective } from './ethics.directive';

describe('EthicsDirective', () => {
  it('should create an instance', () => {
    const directive = new EthicsDirective();
    expect(directive).toBeTruthy();
  });
});
